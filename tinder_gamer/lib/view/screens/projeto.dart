import 'package:flutter/material.dart';

class Projeto extends StatelessWidget{
  const Projeto({super.key});

  Widget build(BuildContext context){
    return Center(
      child: Text("Esse é o projeto dart"),
    );
  }
}